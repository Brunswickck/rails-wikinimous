<div class='articles_list'>
  <ul>
    <% @articles.each do |article| %>
    <!--  <li><%#= task.title %></li>     the hash stops the code from running -->
    <li><%= link_to article.title, article_path(article) %><br><%= article.content %></li>
    <% end %>
  </ul>
</div>
